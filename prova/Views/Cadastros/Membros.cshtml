@using prova.ViewModels;
@model MembroViewModel

@{
    ViewData["Title"] = "Membros";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div style="margin: 0; padding: 0;">
    <div>
        <div class="card blue-grey darken-4" style="position:absolute; bottom: 0; top: 0; margin: 0; width: 100%; height: 100%;">
            <div class="card-content white-text">
                <span class="card-title center-align">@Model.TituloPagina</span>
                <div class="row">
                    <form class="col s12"
                          asp-area="" asp-controller="Membros" asp-action="Cadastrar">
                        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                        <div class="row">
                            @*<div class="input-field">
                                    <i class="material-icons prefix">account_circle</i>
                                    <input asp-for="Nome" id="nomeUsuario" type="text" class="validate" style="color:white;">
                                    <label for="nomeUsuario">First Name</label>
                                    <span asp-validation-for="Nome" class="text-danger"></span>
                                </div>
                                <div class="input-field">
                                    <i class="material-icons prefix">phone</i>
                                    <input asp-for="Telefone" id="telefoneUsuario" type="tel" class="validate" style="color:white;">
                                    <label for="telefoneUsuario">Telephone</label>
                                    <span asp-validation-for="Telefone" class="text-danger"></span>
                                </div>

                                <input asp-for="Id" type="hidden" class="validate" style="color:white;">*@
                        </div>
                        <div class="card-action center-align">
                            @*@if (!Model.EstaVisualizando)
                                {
                                    <a class="card orange accent-4 btn"
                                       asp-area="" asp-controller="Cadastros" asp-action="Index">
                                        Voltar
                                    </a>
                                }
                                else
                                {
                                    <a class="card orange accent-4 btn"
                                       asp-area="" asp-controller="Usuario" asp-action="Usuarios">
                                        Voltar
                                    </a>
                                }
                                @if (!Model.EstaVisualizando)
                                {
                                    <button class="card deep-orange accent-4 btn" type="submit">
                                        Cadastrar
                                        <i class="material-icons right">send</i>
                                    </button>
                                }*@
                        </div>
                        <input asp-for="Id" type="hidden">
                        <input asp-for="EstaVisualizando" type="hidden">
                        <input asp-for="TituloPagina" type="hidden">
                    </form>
                </div>
            </div>

        </div>
    </div>
</div>